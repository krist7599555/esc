<h1>Login</h1>
<form {{on "submit" this.login}}>
  <Input @value={{this.username}} required />
  <Input @value={{this.password}} required />
  <button type='submit'>Login</button>
</form>
<p>{{this.username}} {{this.password}}</p>